'use strict'

import assert from 'assert'

describe('database', () => {
    describe('connect', () => {
        it('should return a list of errors when value not match with schema', () => {
            //
        })
    })
})